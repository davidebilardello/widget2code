BACKEND_PORT=8010
FRONTEND_PORT=3060
HOST=0.0.0.0

ENABLE_MODEL_CACHE=true
USE_CUDA_FOR_RETRIEVAL=true

RETRIEVAL_TOPK=50
RETRIEVAL_TOPM=5
RETRIEVAL_ALPHA=0.8
TIMEOUT=5000

# ========== Perception Models Configuration ==========
# The 6.7b model requires ~14GB VRAM in fp16. The 2.7b model requires ~5.5GB VRAM.
# Optionally install `bitsandbytes` (pip install bitsandbytes) and enable 8bit/4bit to reduce VRAM further.
BLIP2_MODEL_ID=Salesforce/blip2-opt-2.7b
BLIP2_LOAD_IN_8BIT=false
BLIP2_LOAD_IN_4BIT=false

# ========== LLM Configuration ==========
# Thinking Mode Support:
# - qwen3-vl-flash: ✅ Supports thinking=true/false
# - qwen3-vl-plus: ✅ Supports thinking=true/false
# - qwen3-vl-235b-a22b-instruct: ❌ Does NOT support thinking (parameter ignored)
# - qwen3-vl-235b-a22b-thinking: 🔒 ALWAYS uses thinking (cannot disable)

# Qwen
DEFAULT_API_KEY=
DEFAULT_MODEL=qwen3-vl-plus
DEFAULT_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Doubao
DEFAULT_API_KEY=
DEFAULT_MODEL=doubao-seed-1-6-250615
DEFAULT_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# LM Studio (Local Provider)
DEFAULT_API_KEY=lmstudio
DEFAULT_MODEL=local-model
# DEFAULT_BASE_URL=http://localhost:1234/v1

DEFAULT_TEMPERATURE=0.5
DEFAULT_TOP_K=
DEFAULT_TOP_P=
DEFAULT_ENABLE_THINKING=true
DEFAULT_THINKING_BUDGET=500
DEFAULT_VL_HIGH_RESOLUTION=false
# Global default max tokens for all stages (overridable per-stage)
DEFAULT_MAX_TOKENS=

# ========== Stage-specific Overrides ==========
# Empty values will fallback to DEFAULT_* values above

# Layout Detection Stage
LAYOUT_API_KEY=
LAYOUT_MODEL=
LAYOUT_BASE_URL=
LAYOUT_TEMPERATURE=0.7
LAYOUT_TOP_K=
LAYOUT_TOP_P=
LAYOUT_ENABLE_THINKING=
LAYOUT_THINKING_BUDGET=
LAYOUT_VL_HIGH_RESOLUTION=
# Max tokens specifically for layout detection (fallback to DEFAULT_MAX_TOKENS)
LAYOUT_MAX_TOKENS=
# Number of extra attempts when layout returns empty (raw/postProcessed)
# 0 = no retry
LAYOUT_MAX_RETRIES=

# Graph Detection Stage
GRAPH_DET_API_KEY=
GRAPH_DET_MODEL=
GRAPH_DET_BASE_URL=
GRAPH_DET_TEMPERATURE=0.1
GRAPH_DET_TOP_K=
GRAPH_DET_TOP_P=
GRAPH_DET_ENABLE_THINKING=
GRAPH_DET_THINKING_BUDGET=
GRAPH_DET_VL_HIGH_RESOLUTION=

# Graph Generation Stage
GRAPH_GEN_API_KEY=
GRAPH_GEN_MODEL=
GRAPH_GEN_BASE_URL=
GRAPH_GEN_TEMPERATURE=0.3
GRAPH_GEN_TOP_K=
GRAPH_GEN_TOP_P=
GRAPH_GEN_ENABLE_THINKING=false
GRAPH_GEN_THINKING_BUDGET=
GRAPH_GEN_VL_HIGH_RESOLUTION=
# Max tokens specifically for graph generation (fallback to DEFAULT_MAX_TOKENS)
GRAPH_GEN_MAX_TOKENS=

# DSL Generation Stage
DSL_GEN_API_KEY=
DSL_GEN_MODEL=
DSL_GEN_BASE_URL=
DSL_GEN_TEMPERATURE=0.5
DSL_GEN_TOP_K=
DSL_GEN_TOP_P=
DSL_GEN_ENABLE_THINKING=false
DSL_GEN_THINKING_BUDGET=
DSL_GEN_VL_HIGH_RESOLUTION=
# Max tokens specifically for DSL generation (fallback to DEFAULT_MAX_TOKENS)
DSL_GEN_MAX_TOKENS=

CONCURRENCY=100

# ========== Global Rate Limiting ==========
# Limit LLM API requests per minute across all stages
# - Set to 0 to disable rate limiting
# - Default: 60 requests/minute (1 req/sec average)
# - Uses Token Bucket algorithm with smooth refill
REQUESTS_PER_MINUTE=60

# Icon Libraries Configuration
# Default: all 57 available libraries# Minimal (popular):    ICON_LIB_NAMES='["lu", "hi"]'
ICON_LIB_NAMES='["lu", "sf", "ai", "bi", "bs", "cg", "ci", "di", "fa", "fa6", "fc", "fi", "gi", "go", "gr", "hi", "hi2", "im", "io", "io5", "lia", "md", "pi", "rx", "ri", "si", "sl", "tb", "tfi", "ti", "vsc", "wi"]'

# AppLogo Libraries Configuration
# AppLogo is a subset of Icon for brand/app logos
# Default: Simple Icons only (contains 3000+ brand logos)
APPLOGO_LIB_NAMES='["si"]'

MAX_FILE_SIZE_MB=100

SHOW_STAGE_TABLE=true

HF_HOME=

# ========== Evaluation Configuration ==========
# Ground Truth Directory (for widget quality evaluation)
# Required for running evaluation scripts
# Example: GT_DIR=/path/to/ground-truth-images
GT_DIR=
